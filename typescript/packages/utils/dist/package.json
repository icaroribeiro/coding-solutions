{
  "name": "@packages/utils",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean:install": "npm --prefix . ci",
    "build": "swc ./src --out-dir dist --ignore \"**/*.test.ts",
    "postbuild": "cp package*.json dist && npm --prefix dist ci",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "jest"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0"
  }
}
