import { sumResult } from "./foo-lambda";

describe("A", () => { 
  beforeEach(() => {
    jest.resetAllMocks();
  });

  
  describe("", () => {
    it("", async () => {
      expect(sumResult(1,2)).toEqual(3);
    });
    });
  });